{
  "name": "dotfiles-docs",
  "version": "1.0.0",
  "description": "Documentation for dotfiles",
  "scripts": {
    "build": "pip install -r requirements.txt && mkdocs build",
    "dev": "mkdocs serve"
  },
  "dependencies": {
    "@qwen-code/qwen-code": "^0.5.1"
  },
  "devDependencies": {}
}
